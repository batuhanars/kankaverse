# Web (Vite) ortam değişkenleri.
# Dev'de gerek YOK (boş bırak → vite proxy 127.0.0.1:3001'e taşır).
# Prod'da (Vercel) Project Settings → Environment Variables'a ekle:

# Backend kök URL'i (api kök'ten servis eder: /auth, /channels, /socket.io).
# VITE_API_URL=https://api.kankaverse.com
